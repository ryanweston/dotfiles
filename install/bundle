# -----------------------------------------------
# Homebrew bundle file
# Lists all formulae and casks to be installed by
# `brew bundle`. Called from the install script —
# config symlinking is handled separately by stow.
# -----------------------------------------------

brew "neovim"
brew "starship"
brew "zoxide"
brew "fzf"
brew "eza"
brew "lazygit"
brew "fish"
brew "fisher"
brew "stow"
brew "tailscale"

# Casks are macOS only — on Linux these apps are
# installed via the system package manager instead.
if OS.mac?
  cask_args appdir: "/Applications"
  cask "ghostty"
  cask "obsidian"
  cask "raycast"
end
